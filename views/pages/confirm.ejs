<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #1b4332; /* Dark green background */
            color: #ecf0f1; /* Light gray text color */
            text-align: center;
            margin: 0;
            padding: 20px;
            font-size: 20px; /* Increased font size */
        }

        img {
            max-width: 200px;
            margin-bottom: 20px;
        }

        h1, h3 {
            margin-bottom: 10px;
            color: #3498db; /* Blue color for headings */
        }

        #confirmation-container {
            background-color: #fff;
            color: #2c3e50; /* Dark blue-gray text color */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
            margin-top: 20px;
            width: 500px;
            margin-left: auto;
            margin-right: auto;

        }

        p {
            margin: 5px 0;
            color: #34495e; /* Darker blue-gray text color for paragraphs */
        }

        hr {
            border: 0;
            height: 1px;
            background: #3498db; /* Blue color for the horizontal rule */
            margin: 10px 0;
        }

        #total-container {
            margin-top: 20px;
            color: #e74c3c; /* Red color for total amount */
            font-weight: bold;
        }

        button {
            background-color: orange; /* Green color for the button */
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
            margin-left: 20px;
            width: 90px;
            text-align: center;
            font-size: 25px;
            margin-right: 10px;
        }

        button:hover {
            background-color: #27ae60; /* Darker green on hover */
        }
    </style>
</head>
<body>
    <img src="../../public/images/mcdonalds-logo.png" alt="McDonald's Logo">
    <h1>Is This Order Correct?</h1>
    <h3>My Eat In Order</h3>
    <div id="confirmation-container">
        <% list.forEach(item => { %>
            <p style="display: inline-block;margin-right: 185px;width: 200px;" ><%= item.name %></p>
            <p style="display: inline-block;" >Price: <%= item.price %></p>
            <p style="margin-right: 280px;" >Quantity: <%= item.quantity %></p>
            <hr>
        <% }) %>

        <p style="margin-right: -315px;" >Total Amount: <%= total %>  </p>
    </div>
    <div id="total-container">
        
        <button style="background-color: red;" onclick="window.location.href='/productss'" >No</button>
        <% if(mode==='card'){ %>
            <form style="display: inline-block;" action="/create-checkout-session" method="POST">
                <button type="submit">Yes</button>
            </form>
        <% }else{ %>
            <button onclick="window.location.href='/cash'" >Yes</button>
        <% } %>
    </div>
</body>
</html>
